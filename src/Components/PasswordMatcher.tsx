import React, { useState } from 'react';

export default function PasswordMatcher() {
    const [] = useState()

    return (
        <div>
            {/* <input type='password' onChange={handlePasswordChange} value={password} />
            <input type='password' onChange={(handleRetypePasswordChange)} value={reTypedPassword} />

            <span>{isValid ? "Awesome": "Please check again :("}</span> */}
        </div>
    )
}